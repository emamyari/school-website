import axios from 'axios';
function Response(FilePath) {
    // axios.get("http://127.0.0.1:8000/" + String(FilePath)).then(Response => Response.data)
    return "http://127.0.0.1:8000/"
}
export default Response;